#include "InventorySlotWidget.h"

// Seule la version _Implementation doit être définie dans le .cpp
void UInventorySlotWidget::UpdateSlot_Implementation(const FInventorySlot& SlotData)
{
	// Logique de mise à jour C++ par défaut (ex: textures, texte)
	// Cette fonction sera exécutée ou surchargée dans le Blueprint UMG
}